@model IEnumerable<ProyectoFacturacion.FactTipoPago>

@{
    ViewData["Title"] = "Index";
}
<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdTipoPago)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdTipoPago)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                    @Html.ActionLink("Details", "Details", new {  Id = item.IdTipoPago }) |
                @Html.ActionLink("Delete", "Delete", new {  Id = item.IdTipoPago })
            </td>
        </tr>
}
    </tbody>
</table>
